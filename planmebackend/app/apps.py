"""App config for planmebackend app."""
from django.apps import AppConfig


class PlanmebackendappConfig(AppConfig):
    """App config for planmebackend app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "planmebackend.app"
